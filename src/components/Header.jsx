import logo from './../logo.png';

const Header = () => {
    return (
        <header className='header'>
            <img className='logo'
                 src={logo}/>
        </header>
    )
};

export default Header;